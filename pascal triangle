#include<stdio.h>
int main()
{
	int r,c,n,s;
	printf("Enter the value:");
	scanf("%d",&n);
	
	for(r=1;r<=n;r++)
	{
		s=1;
		for(c=1;c<=r;c++)
		{
			printf("%d ",s);
			s= s*(r-c) / c;
		}
		
		printf("\n");
	}
	
}
